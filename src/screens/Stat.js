import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const Stat = () => {
  return (
    <View>
      <Text>Stat</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
